<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sbsromero.proyectosara">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.monitor.HomeMonitorActivity" />
        <activity
            android:name=".activities.monitor.CitasPendientes"
            android:parentActivityName=".activities.monitor.HomeMonitorActivity" />
        <activity
            android:name=".activities.monitor.CancelarCitas"
            android:parentActivityName=".activities.monitor.HomeMonitorActivity" />
        <activity
            android:name=".activities.monitor.DetalleCitasPendientes"
            android:parentActivityName=".activities.monitor.CitasPendientes" />
        <activity android:name=".activities.monitor.DetalleCitasAsignadas"></activity>
    </application>

</manifest>